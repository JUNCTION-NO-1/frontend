interface DinosaurResponse {
  id: string;
  level: number;
  totalTime: number;
}

interface TimerEndResponse {
  totalTime: number;
  level: number;
}

interface RankingResponse {
  ranking: number;
  nickname: string;
  totalTime: number;
  level: number;
}
